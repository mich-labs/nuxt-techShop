services:
  nuxtie:
    image: nuxtie:latest
    container_name: nuxtie
    build: .
    networks:
      - www
    restart: unless-stopped
    profiles: ["root","www","nuxtie"]
    env_file: .env
  
networks:
  www:
    external: true
