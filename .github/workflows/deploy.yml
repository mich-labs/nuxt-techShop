name: Deploy nuxt

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: [self-hosted, rpi]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: set ENV
        run: |
          cat > .env << EOF
          STRAPI_URL=${{ secrets.STRAPI_URL }}
          STRAPI_PUBLIC_ACCESS_TOKEN=${{ secrets.STRAPI_PUBLIC_ACCESS_TOKEN }}
          NODE_ENV=production
          EOF

      - name: Rebuild container
        run: |
          docker compose up nuxtie -d --build
