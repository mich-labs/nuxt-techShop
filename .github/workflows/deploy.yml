name: Deploy nuxt

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: [self-hosted, rpi]

    steps:
      - uses: actions/checkout@v6

      - run: |
          docker compose up nuxtie -d --build